---
services:
  freqtrade:
    # Requires Docker Desktop (WSL2 backend) with NVIDIA GPU support enabled.
    gpus: all
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
      - OMP_NUM_THREADS=8
